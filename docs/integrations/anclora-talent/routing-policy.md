# Routing Policy

Priority:

1. Local Agent when paired, available and operation advertised.
2. Service worker when Local Agent unavailable or user accepts cloud processing.
3. Browser for lightweight image operations.

Every UI action must display the processing mode used: local, service or browser.
